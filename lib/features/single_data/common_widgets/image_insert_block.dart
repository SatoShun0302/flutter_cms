import 'package:flutter/material.dart';

/// 記事新規作成,更新時用ウィジェット[画像ブロック].
class ImageInsertBlock extends StatelessWidget {
  const ImageInsertBlock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
